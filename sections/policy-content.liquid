<section class="container fade-up">
  <div class="section-card">
    <h1 class="section-title">{{ page.title }}</h1>
    <div class="section-subtitle" style="color: var(--text-primary);">
      {{ page.content | default: "Add your policy content here." }}
    </div>
  </div>
</section>
{% schema %}
{
  "name": "Policy content",
  "settings": []
}
{% endschema %}
